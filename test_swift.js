require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGO_URI || process.env.MONGODB_URI).then(async () => { try { const SwiftMessage = require('./src/models/SwiftMessage'); const messages = await SwiftMessage.find().sort({ generatedAt: -1 }).lean(); console.log('Found messages:', messages.length); const enriched = messages.map(msg => { try { const SwiftRenderer = require('./src/engine/swift/renderers/SwiftRenderer'); return SwiftRenderer.renderDisplay(msg.messageType, msg.fieldMap || {}, msg.senderBIC, msg.receiverBIC); } catch (e) { console.error('Render error:', e.message); return msg.messagePayload; } }); console.log('Enriched successful!'); } catch (e) { console.error('Fatal error:', e); } process.exit(0); })
